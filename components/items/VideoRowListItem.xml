<?xml version="1.0" encoding="utf-8" ?>
<!-- VideoRowListItem — card component for RowList rows
     Item size: 350×230 (poster 350×220 + info bar 350×50)
     Duration pill bottom-right of poster -->
<component name="VideoRowListItem" extends="Group">
    <script type="text/brightscript" uri="pkg:/components/items/VideoRowListItem.brs" />

    <interface>
        <field id="itemContent"  type="node"  onChange="onItemContentChanged" />
        <field id="focusPercent" type="float" onChange="onFocusPercentChanged" />
    </interface>

    <children>
        <!-- Poster background — prevents white flash while image loads -->
        <Rectangle id="posterBg" width="350" height="220" color="0x1A122BFF" />

        <!-- Poster image -->
        <Poster id="poster" width="350" height="220" loadDisplayMode="scaleToZoom" />

        <!-- Focus border ring -->
        <Rectangle id="focusBorder"
                   width="308" height="188" translation="[-4, -4]"
                   color="0x9E46EAFF" visible="false" blendingEnabled="true" />

        <!-- Focus gradient overlay -->
        <Rectangle id="focusGradient"
                   width="350" height="90" translation="[0, 90]"
                   color="0x662D9190" visible="false" blendingEnabled="true" />

        <!-- Title bar -->
        <Rectangle id="titleBg" width="350" height="50" translation="[0, 220]"
                   color="0x161024FF" />

        <Label id="title" width="284" height="26"
               translation="[8, 184]"
               font="font:SmallBoldSystemFont" color="0xE5E7EBFF"
               horizAlign="left" vertAlign="center"
               truncateOnDelimiter=" " />

        <Label id="category" width="210" height="18"
               translation="[8, 208]"
               font="font:SmallSystemFont" color="0x9CA3AFFF"
               horizAlign="left" />

    </children>
</component>
